id: energyutils
name: EnergyUtils
version: "0.0.0"
description: A datapack that add somes energy component to minecraft
author: AirDox_


data_pack:
  load: [.]
resource_pack:
  load: [.]


extend: ["common.yaml"]
